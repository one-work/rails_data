<%=
  tr_tag(
    model,
    {
      action: url_for(action: 'rebuild', id: model.id),
      icon: 'recycle',
      title: t('.rebuild.title'),
      confirm: t('.rebuild.confirm')
    },
    icons: ['edit', 'destroy']
  ) { yield }
%>
